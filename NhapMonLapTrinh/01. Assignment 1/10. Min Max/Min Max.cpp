// Lưu ý không dùng phép dịch bit, không dùng cấu trúc rẽ nhánh
#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int a, b;
    cin >> a >>b;
    cout << "max = " << max ( a, b ) << "\n";
    cout << "min = " << min(a,b);
}
